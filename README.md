# 📘 Diary Manager — C++ / SQLite / CMake

Консольное приложение для управления заметками в формате дневника.  
Реализовано на C++ с архитектурой MVC, использует SQLite для хранения данных.  
Поддерживает кроссплатформенную сборку через CMake и скрипты для Windows/Linux.

---

## 📁 Структура проекта

```
.
├── .gitignore              # Исключения для Git
├── build.bat               # Сборка для Windows
├── build.sh                # Сборка для Linux/macOS
├── CMakeLists.txt          # Конфигурация CMake
├── README.md               # Документация проекта
│
├── SQLiteCpp/              # Внешняя библиотека SQLiteCpp
│
└── src/                    # Исходный код проекта
    ├── controller.*        # Контроллер: логика взаимодействия
    ├── cwuser.*            # Ввод/вывод пользователя
    ├── db_manager.*        # Работа с файлами БД
    ├── model.*             # Модель: бизнес-логика
    ├── note_manager.*      # CRUD для заметок
    ├── structures.h        # Структура заметки
    ├── table_manager.*     # Работа с таблицами
    ├── view.*              # Отображение данных
    └── main.cpp            # Точка входа
```

---

## 🚀 Возможности

- 🗂 **Управление базами данных**
  - Создание, удаление, переименование, подключение

- 📄 **Управление таблицами**
  - Создание, удаление, переименование, подключение

- 📝 **Работа с заметками**
  - Поиск по ключевым словам или ID
  - Создание, редактирование, удаление
  - Просмотр полной заметки

---

## 🧱 Архитектура

Проект реализует паттерн **MVC**:

| Компонент   | Назначение |
|-------------|------------|
| `Model`     | Работа с SQLite через менеджеры БД, таблиц и заметок |
| `View`      | Форматирование и вывод данных |
| `Controller`| Управление логикой приложения |
| `CWUser`    | Ввод/вывод пользователя |

---

## 🛠️ Сборка

### 🔧 Требования

- `C++17`
- `CMake ≥ 3.10`
- `SQLiteCpp` (включена в проект)

### 💻 Windows

```bat
build.bat
```
### 🐧 Linux/macOS

```bash
chmod +x build.sh
./build.sh
```